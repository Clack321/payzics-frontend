/* eslint-disable array-callback-return */
// import routes from '../../../constants/routes';
// import buildMutation from './buildMutation';

// function makeCustomRoute() {
//   routes.map(route => {
//     if (route.type === 'GET') {
      
//     } else if (route.endpoint === 'authentication' ) {

//     } else {
//       return (builder, type, queryObj) => ({ ...body }) => buildMutation({route, id: routeParams, body})
//     }
//   })
// }