import buildService from './buildService';
import buildServices from './buildServices';
import buildFeathersQuery from './buildFeathersRequests';
import buildCache from './buildCache';

export {
  buildService,
  buildServices,
  buildFeathersQuery,
  buildCache,
};

export default {
  buildService,
  buildServices,
  buildFeathersQuery,
  buildCache,
};