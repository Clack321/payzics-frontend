// import { createApi } from '@reduxjs/toolkit/query'

// const buildAsyncAction = ({ slice, actionStr }) => {
//   if (prepare) {
//     return createAction(`${slice}/${actionStr}`, prepare);
//   } else {
//     return createAction(`${slice}/${actionStr}`);
//   }
  
// };

// export default buildAsyncAction;